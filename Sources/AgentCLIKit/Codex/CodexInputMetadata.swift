/// Codex-specific metadata keys for richer user input.
public enum CodexInputMetadata {
    /// Marks a Codex input as an app shot, so managed app-shot policy can be enforced before send.
    public static let isAppshot = "codex_input_is_appshot"
}
